<!DOCTYPE html>
<html dir="ltr" lang="fr-FR">
<head>
	<link rel="stylesheet" href="{{ asset('bundles/fulguriolightcms/css/bootstrap.min.css') }}" />
	<link rel="stylesheet" href="{{ asset('bundles/fulguriolightcms/css/styles-admin.css') }}" />
	<script type="text/javascript" src="{{ asset('bundles/fulguriolightcms/js/tiny_mce/tiny_mce_popup.js') }}"></script>
</head>
<body>
<script type="text/javascript">
var URL = "/{{ fileUrl }}";
var win = tinyMCEPopup.getWindowArg("window");
win.document.getElementById(tinyMCEPopup.getWindowArg("input")).value = URL;
if (typeof(win.ImageDialog) != "undefined") {
	if (win.ImageDialog.getImageData) {
		win.ImageDialog.getImageData();
	}
	if (win.ImageDialog.showPreviewImage) {
		win.ImageDialog.showPreviewImage(URL);
	}
}
tinyMCEPopup.close();
</script>
</body>
</html>